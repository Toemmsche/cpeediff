- obscure Attribute changes (wait or probability)
- deep inserts (multiple layers, thin subtrees)
- invalid trees
- X - mappings
- test EXACT_EDIT_SCRIPT using patcher
- leaf substitution (are parents still matched) ?
- low leaf sim threshold
- high leaf sim threshold
- n² poath tree for matching



unit tests:
- no root (empty tree)
- no init script
- one init script
- one root
- changed indentation / whitespaces

TESTS
for each - insertion/move/update/delete

leaf
subtree
arg/property

special cases for each operation:
insert:
- worse copy
- same label

move:
- interparent
- nested
- nested with insert

update:
- code update (still matches)
- endpoint update
- endpoint stays the same

delete:


standard cases:
- small mixed
- identical
- generated
- totally different

edge cases:
- only root
- leaf node substitution
- cross matching
- node replacement
