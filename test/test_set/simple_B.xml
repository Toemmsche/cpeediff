<description xmlns="http://cpee.org/ns/description/1.0">
    <escape attr="val"></escape>
    <parallel>
        <loop>
            <stop></stop>
        </loop>
    </parallel>
    <choose>
        <otherwise>
            <manipulate>data.time++;</manipulate>
        </otherwise>
    </choose>
    <alternative condition="data.costs < 0">
        <loop condition="data.persons > 0">
            <terminate></terminate>
        </loop>
    </alternative>
</description>